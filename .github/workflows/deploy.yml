name: Build & Deploy

on:
  push:
    branches:
      - "main"
      
jobs: 
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      # - name: Set up QEMU
      #   uses: docker/setup-qemu-action@v2
      
      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v2
      
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          # platforms: linux/amd64,linux/arm64
          file: ./microservices/financial-service-web/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/financial-service-web:latest
      
      # Then remote to swarm host server and manually deploy by using shell script
      
      
#       - name: Deploy to Swarm
#         uses: sagebind/docker-swarm-deploy-action@v2
#         with:
#           remote_host: ssh://${{ secrets.REMOTE_USER }}@${{ secrets.REMOTE_HOST }}
#           ssh_private_key: ${{ secrets.DOCKER_SSH_PRIVATE_KEY }}
#           ssh_public_key: ${{ secrets.DOCKER_SSH_PUBLIC_KEY }}
#           args: stack deploy --compose-file stack.yaml app
          
